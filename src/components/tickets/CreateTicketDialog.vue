Tag mutability settings new Control whether tags can be edited to help maintain consistent, secure,
and reliable deployments. Learn more⁠. All tags are mutable Default Tags can be changed to reference
a different image. This allows you to retarget a tag without creating a new one. All tags are
immutable Tags cannot be updated to point to a different image after creation. This ensures
consistency and prevents accidental changes. Specific tags are immutable Define specific tags that
cannot be updated after creation using RegEx values.
